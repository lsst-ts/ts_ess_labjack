device_type: T4
connection_type: USB
identifier: test.foo.bar
# This max_frequency is assumed in test_acceleration_data_client.py, and it produces
# nice round PSD frequencies, which makes the data easier to understand.
max_frequency: 800
accelerometers:
  - sensor_name: alpha
    location: somewhere
    analog_inputs: [0, 2, 5]
    offsets: [0, -0.1, 1]
    scales: [1, 0.1, 2]
